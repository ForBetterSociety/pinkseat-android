<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hyeong.pinkseat">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/user_icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/user_icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:windowSoftInputMode="adjustPan">
        <activity android:name=".MainActivity"
            android:label="회원가입"  />
        <activity android:name=".ConfirmLoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".JoinActivity" />
        <activity
            android:name=".Main2Activity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".SubwayActivity" />
        <activity android:name=".SeatActivity" />
        <activity android:name=".SubwayLineActivity" />
        <activity android:name=".LoginActivity"></activity>
        <activity android:name=".WebView.TrafficSubwayDetail" />
    </application>

</manifest>